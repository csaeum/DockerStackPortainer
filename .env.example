# Portainer Stack Configuration
# Kopiere diese Datei zu .env und passe die Werte an

# Docker Compose Projekt-Name (wird für Container-Namen und Traefik-Labels verwendet)
COMPOSE_PROJECT_NAME=portainer

# Traefik Host-Rule (passe die Domain an deine Umgebung an)
HOSTRULE=Host(`portainer.example.com`)

# Traefik Proxy Network Name
PROXY_NETWORK=traefik_proxy_network
